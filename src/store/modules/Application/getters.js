const loading = (state) => {
  return state.loading;
};

const title = (state) => {
  return state.title;
};

const error = (state) => {
  return state.error;
};
export default {
  loading,
  title,
  error,
};
